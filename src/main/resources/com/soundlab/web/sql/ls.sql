
select *from MUSIC
;
  SELECT
      A.ARTIST_SEQ ,
      A.ARTIST_NAME, 
     A.SEX, 
     A.BIRTH, 
     A.DEBUT, 
     A.NATION,
      A.GROUP_NAME,
      A.INTRO1, 
     A.INTRO2,
     (SELECT CONCAT(IMG_NAME,'.', EXT)
   FROM IMG WHERE SEQ LIKE AL.ALBUM_SEQ )AS IMG
FROM  ARTIST A 
  JOIN ARTIST A ON AL.ALBUM_SEQ LIKE A.ARTIST_SEQ
  WHERE A.ARTIST_NAME LIKE '선미'
     ; 
     
     
     
SELECT @RNUM := @RNUM + 1  AS NO , A.*
FROM 
VIEW_RECORD V,

SELECT
 @RNUM := @RNUM + 1  AS NO , A.*,
	V.VIEW_DATE

FROM 
VIEW_RECORD V,

(SELECT  
  AL.ALBUM_TITLE,
  AR.ARTIST_NAME,
  MUS.MUSIC_TITLE,
  

 COUNT(SEQ_GROUP)AS VIEW_COUNT,
  (SELECT CONCAT(IMG_NAME,'.', EXT)
    FROM IMG 
    WHERE SEQ LIKE AL.ALBUM_SEQ )IMG 
FROM  MUSIC MUS
  JOIN VIEW_RECORD V ON V.SEQ_GROUP LIKE MUS.MUSIC_SEQ
  JOIN ALBUM AL ON MUS.ALBUM_SEQ LIKE AL.ALBUM_SEQ
  JOIN ARTIST AR ON MUS.ARTIST_SEQ LIKE AR.ARTIST_SEQ


GROUP BY SEQ_GROUP
ORDER BY VIEW_COUNT DESC LIMIT 50)AS A,
( SELECT @RNUM := 0 )AS B


;